import React from "react";

/* export const Artist = (props) => {
    {props.artists.name.map(artists => {
        return (
            <>
             {artists.name}
            </>
        )
    })}

}
 */

import React from "react";
import "./Header.css"

const Header = (props) => {
    return (
        <div>
            <h1 className="Header">New Albums & Singles</h1>
        </div>
    );
};

export default Header;
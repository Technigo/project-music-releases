import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import { App } from './App'

ReactDOM.render(<App />, document.getElementById('root'))
// Connecting the HTML element root into the react component file index.js and rendering the virtual DOM
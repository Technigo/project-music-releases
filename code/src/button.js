import React from 'react'

export const Button = ({ href, items }) => {
  // console.log('data in the button', props)
  // const href = props.href
  // const items = props.items

  return <button className="btn">{href}</button>

}
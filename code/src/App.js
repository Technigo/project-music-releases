import React from 'react'
import data from './data.json'

import { Article } from './components/Article'

console.log(data)

const testar = (data) => {
  fetch = data
  .then((res) => res.json())
  .then((data) => {


  }
    
  );
}